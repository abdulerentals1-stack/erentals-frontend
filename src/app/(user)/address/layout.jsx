export const metadata = {
  title: "My Addresses | e-Rentals",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AddressLayout({ children }) {
  return <>{children}</>;
}
