export const metadata = {
  title: "My Quotations | e-Rentals",
  robots: {
    index: false,
    follow: false,
  },
};

export default function QuotationLayout({ children }) {
  return <>{children}</>;
}
