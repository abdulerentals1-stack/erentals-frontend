export const metadata = {
  title: "Quotation Checkout | e-Rentals",
  robots: {
    index: false,
    follow: false,
  },
};

export default function QuotationCheckoutLayout({ children }) {
  return <>{children}</>;
}
